---
const quotes = [
  "Machines learn. Humans imagine.",
  "Code is poetry written for machines.",
  "ML is not magic - it's mathematics with intent."
];

const randomQuote = quotes[Math.floor(Math.random() * quotes.length)];
---

<div class="creative-quote">
  "{randomQuote}"
</div>
